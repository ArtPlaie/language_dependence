# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Single provider gateway: OpenRouter (https://openrouter.ai/).
OPENROUTER_API_KEY=

# Optional: set a custom base URL (defaults to https://openrouter.ai/api/v1).
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
